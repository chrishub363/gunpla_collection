<div class="flex gap-4 p-3 <%= index.odd? ? 'bg-surface' : 'bg-panel' %> hover:bg-elevated transition-colors"
     data-filter-target="row"
     data-filter-text="<%= kit.filter_text %>">

  <%# Box art %>
  <div class="shrink-0 w-36 h-24 bg-border rounded overflow-hidden">
    <img src="<%= kit_image_path(kit) %>" alt="<%= kit.title %>"
         data-action="click->lightbox#open"
         data-lightbox-src-param="<%= kit_image_path(kit) %>"
         class="w-full h-full object-cover cursor-zoom-in">
  </div>

  <%# Kit info %>
  <div class="flex-1 min-w-0 flex flex-col justify-center">
    <div class="font-medium text-ink min-w-0">
      <span class="truncate min-w-0 block"><%= kit.title %></span>
    </div>
    <%= kit_attribute_tags(kit) %>
    <% if kit.topic.present? %>
      <div class="text-xs text-dim mt-0.5 truncate"><%= kit.topic %></div>
    <% end %>
    <%= kit_source_links(kit) %>
  </div>

  <%# Status badge %>
  <div class="shrink-0 flex items-center">
    <%= status_badge(kit.status, link: true) %>
  </div>

</div>
